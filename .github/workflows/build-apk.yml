name: Construir APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Descargar proyecto
        uses: actions/checkout@v4

      - name: Descomprimir proyecto
        run: unzip -q GestionesVehiculares_APK_Punto3_GITHUB_BUILD.zip

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Dar permisos a Gradle
        run: chmod +x GestionesAPK/gradlew

      - name: Compilar APK
        working-directory: GestionesAPK
        run: ./gradlew assembleDebug

      - name: Guardar APK
        uses: actions/upload-artifact@v4
        with:
          name: GestionesVehiculares-APK
          path: GestionesAPK/app/build/outputs/apk/debug/app-debug.apk
